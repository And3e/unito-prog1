#include "func.h"

void es1() {
  ciao();
}

int main() {
  
  
  // cast-type *ptz;
  // ptz = (cast-type *) malloc (byte-size)
  // --> sizeof
  
  // malloc restituisce *void (fare poi il cast)

  // int *x;
  // x = (int *) malloc (sizeof (int));


  // free(x);

  // ---------------
  
  // struct
  // unione
  
  // ---------------

  // STACK - HEAP
}

